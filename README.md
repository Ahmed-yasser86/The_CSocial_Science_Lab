# Graph RAG Agent

## Project structure

- Ingestion_Pipline: ingestion and embedding utilities
- RetrievalPipeline: graph-based retrieval workflow
- SocialScienceResearch: YouTube computational social science module (collection, sampling, analytics)
- main.py: unified entry point

## Running

From the repository root:

```bash
python main.py
```

## Environment

Make sure the virtual environment is active and the required packages are installed.
