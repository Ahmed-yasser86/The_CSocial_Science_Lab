# Graph RAG Agent — Computational Social Science Research Workbench

> **Three systems, one repository, one research workflow.** YouTube recommendation & audience network analysis with full provenance (observed, never estimated), a resumable Graph-RAG intelligence agent, and a resilient RAG ingestion pipeline.

![Docs](https://img.shields.io/badge/docs-MkDocs%20Material-blue)
![OpenAPI](https://img.shields.io/badge/OpenAPI-160%20paths-blue)
![Contract](https://img.shields.io/badge/contract-guarded-green)
![License](https://img.shields.io/badge/license-MIT-lightgrey)

**→ Docs are 3-track (GitHub Pages):**
- [**For Recruiters** (2 min)](docs/for-recruiters/index.md)
- [**For Researchers** (15 min)](docs/for-researchers/index.md)
- [**For Developers** (5 min to run)](docs/for-developers/quickstart.md)

Every claim resolves to committed `file:line` — no git-ignored planning docs. The API source of truth is `SocialScienceResearch/api/openapi.json` (generated by `scripts/dump_openapi.py`, guarded by `tests/test_openapi_snapshot.py`).

---

## Choose your track

| Track | Time | What you get |
|---|---|---|
| [**Recruiters**](docs/for-recruiters/index.md) | 2 min | Problem → solution → rigor → stack → role |
| [**Researchers**](docs/for-researchers/index.md) | 15 min | Methodology · reproducibility · two network families · S1–S5 echo-chamber signals · ethics · citation |
| [**Developers**](docs/for-developers/quickstart.md) | 5 min | Postgres → backend → frontend → first graph — full 160-path reference |

---

## Project structure — what is actually shipped

```
SocialScienceResearch/   # A. CSS Research Workbench (primary)
  acquisition/ domain/ persistence/ services/ api/ config/ ui/
    # api/app.py:1299 create_app()  → api/openapi.json 160 paths
    # 30+ services: network, sampling, echo-chamber, commenter-overlap, datasets, ...
    # ui/: Next.js 16 frontend (Lab, network visualizer, agent console, AI Config)
RetrievalPipeline/       # B. Graph-RAG Intelligence Agent
  Graph/                 #   LangGraph StateGraph, 5 nodes (intelligence_graph.py:506)
  agent_server.py        #   agent router → /api/agent/*, /copilotkit/*, /health (:461)
Ingestion_Pipline/       # C. Ingestion library — Tavily → chunk → Qdrant
  ingestion_service.py   #   EmbedDocumentsToVectoreDb() (:162)
gpt-researcher/          # vendored research engine (pyproject.toml)
main.py                  # unified entry point
docker-compose.yml       # Postgres 16 (social_science DB)
.env.example             # env template (database, LLM providers)
```

## Canonical research journey

```
Select a target → Collect (provenance per run) → Understand (coverage) →
Explore → Sample (seed=42) → Network (recommendation + audience) →
Test differences → Export (GraphML/CSV/JSON/XLSX)
```

Every feature that advances this journey exists as a real endpoint in `SocialScienceResearch/api/openapi.json` — nothing aspirational.

---

## Running — 3 commands

```bash
# 1. Postgres (auto-creates social_science)
docker compose up -d

# 2. Backend — same FastAPI serves CSS + agent
uvicorn SocialScienceResearch.api:create_app --factory --host 0.0.0.0 --port 8000

# 3. Frontend
cd SocialScienceResearch/ui && npm install && npm run dev
```

Docs locally: `pip install mkdocs-material && mkdocs serve`.

---

## Environment

Copy `.env.example` → `.env`. Key vars:

| Variable | Default | Purpose |
|---|---|---|
| `SOCIAL_DATABASE_URL` | `postgresql://postgres:123456@localhost:5432/social_science` | Postgres |
| `SOCIAL_SAMPLING_SEED` | `42` | Deterministic sampling |
| `SOCIAL_COLLECT_TRANSCRIPTS` | `False` | Transcripts strictly opt-in |
| `OPENAI_API_KEY`, `STRATEGIC_LLM`, `SMART_LLM`, `FAST_LLM`, `CHAT_MODEL`, `EMBEDDING` | — | Agent providers (editable in-app via AI Config) |

---

## Honesty

- **Observed, never estimated.** Missing data is `available | missing | unsupported` (`SocialScienceResearch/domain/enums.py:137`), never zeroed.
- **Deterministic.** Sampling and community detection use `seed=42`; permutation tests are seeded and reproducible.
- **Validated.** The centrality battery matches `networkx` on Zachary's Karate Club to `1e-6` (`tests/test_centrality_benchmark.py`).
- **Contract is law.** `api/openapi.json` is generated and CI-guarded; docs never invent a path.

## Citation

See [Researchers → Citation](docs/for-researchers/citation.md). `CITATION.cff` at repo root.

## License

MIT.
